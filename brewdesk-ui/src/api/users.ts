/**
 * TODO: No /api/users or /api/admin/users endpoint is implemented in the backend yet.
 * The backend secures /api/admin/** for ROLE_ADMIN but no controller exists.
 * When the backend adds these endpoints, implement them here.
 *
 * Expected future endpoints (once backend implements them):
 *   GET  /api/admin/users          → User[]
 *   GET  /api/admin/users/{id}     → User
 *   POST /api/admin/users          → User
 *   PUT  /api/admin/users/{id}     → User
 */

export {}
